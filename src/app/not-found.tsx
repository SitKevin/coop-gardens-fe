import Custom404 from "@/components/errors/custom_404";

export default function NotFoundPage() {
  return <Custom404 />;
}
// không thể di chuyển file này sang thư mục components vì nó sẽ bị lỗi && không được đổi tên file này
